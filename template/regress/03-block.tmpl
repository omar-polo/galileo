{! #include <stdlib.h> !}

{{ define base(struct template *tp, const char *title) }}
{! char *foo = NULL; !}
<!doctype html>
<html>
	<head>
		<title>{{ title }}</title>
	</head>
	<body>
		<h1>{{ title }}</h1>
		{{ render tp->tp_body }}
	</body>
</html>
{{ finally }}
{! free(foo); !}
{{ end }}
